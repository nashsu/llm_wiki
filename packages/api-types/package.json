{
  "name": "@llm-wiki/api-types",
  "version": "0.6.6",
  "private": true,
  "description": "LLM Wiki REST API contract — single source of truth (issue #20): the Zod schemas the server validates with, the z.infer types the web client consumes, and the stable error codes. Built to JS + .d.ts; no hand-mirroring.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^9.1.0",
    "zod": "^4.4.3"
  }
}
